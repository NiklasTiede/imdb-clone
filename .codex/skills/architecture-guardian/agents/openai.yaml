interface:
  display_name: "Architecture Guardian"
  short_description: "Review architecture, Modulith boundaries, and Kubernetes readiness."
  default_prompt: "Use $architecture-guardian to review this repository architecture, including Kubernetes readiness, and produce a read-only guardian report."

policy:
  allow_implicit_invocation: false
