apiVersion: apps/v1
kind: Deployment
metadata:
  name: imdb-clone-backend
  namespace: imdb-clone
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: imdb-clone-backend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: imdb-clone-backend
    spec:
      containers:
        - name: backend
          image: niklastiede/imdb-clone-backend:v0.3.0@sha256:5564938a5a3a1c13cb7f31d9b8f5598451a30742795b94309b08a686b79e6870
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
            - name: management
              containerPort: 8081
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: prod
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://imdb-clone-postgresql.databases.svc.cluster.local:5432/movie_db
            - name: OPENSEARCH_URIS
              value: http://imdb-clone-opensearch.databases.svc.cluster.local:9200
            - name: IMDB_CLONE_MEDIA_STORAGE_URI
              value: http://imdb-clone-rustfs.databases.svc.cluster.local:9000
            - name: SPRING_APPLICATION_JSON
              value: |-
                {
                  "management": {
                    "metrics": {
                      "distribution": {
                        "percentiles-histogram": {
                          "http.server.requests": true
                        }
                      }
                    }
                  }
                }
          envFrom:
            - secretRef:
                name: backend-runtime
          readinessProbe:
            tcpSocket:
              port: management
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 12
          livenessProbe:
            tcpSocket:
              port: management
            initialDelaySeconds: 90
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            requests:
              cpu: 250m
              memory: 768Mi
            limits:
              cpu: "1"
              memory: 1536Mi
---
apiVersion: v1
kind: Service
metadata:
  name: imdb-clone-backend
  namespace: imdb-clone
  annotations:
    argocd.argoproj.io/sync-wave: "5"
  labels:
    app.kubernetes.io/name: imdb-clone-backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: imdb-clone-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: management
      port: 8081
      targetPort: management
