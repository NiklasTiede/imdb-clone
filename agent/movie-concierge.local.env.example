# Copy this file to ../.secrets/movie-concierge.local.env and replace the placeholder.
# The service reads this exact ignored file only; do not source it or commit the copy.
OPENAI_API_KEY=replace-with-a-restricted-development-key
